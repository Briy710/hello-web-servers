extends layout

block content
  .show-file-name
    | header that shows file name
  .view-container
    .markdown-view
        h3 Markdown
        textarea(id='input')
        button(id='submit') Submit
    .preview-view
      h3 Preview
      
